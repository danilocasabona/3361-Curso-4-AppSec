repos:
- repo: local
  hooks:
    - id: test-hook
      name: Test Hook
      description: Apenas para testar se o pre-commit está rodando.
      entry: bash -c 'echo "🔍 O pre-commit está funcionando!"'
      language: system
      always_run: true
      stages: ["pre-commit"]